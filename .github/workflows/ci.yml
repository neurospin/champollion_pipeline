name: CI — Lint & Test

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    environment: ci
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache
          df -h /

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Pixi
        uses: prefix-dev/setup-pixi@v0.9.4
        with:
          pixi-version: v0.59.0
          manifest-path: pixi.toml
          auth-host: prefix.dev
          auth-token: ${{ secrets.PREFIX_DEV_TOKEN }}


      - name: Debug Pixi environment location
        run: |
            echo "Listing ~/.pixi:"
            ls -la ~/.pixi || echo "No global Pixi cache found"
            echo "Listing ./.pixi:"
            ls -la ./.pixi || echo "No local Pixi cache found"
            echo "Listing ~/.pixi/envs:"
            ls -la ~/.pixi/envs || echo "No global Pixi envs found"
            echo "Listing ./.pixi/envs:"
            ls -la ./.pixi/envs || echo "No local Pixi envs found"

      - name: Cache Pixi environment
        uses: actions/cache@v5
        id: cache-pixi
        with:
          path: |
            ~/.pixi/envs
            ./.pixi/envs
          key: ${{ runner.os }}-pixi-${{ hashFiles('pixi.toml', 'pixi.lock') }}
          restore-keys: |
            ${{ runner.os }}-pixi-

      - name: Install dependencies
        run: pixi run install-all

      - name: Flake8 (non-blocking)
        continue-on-error: true
        run: pixi run flake8 src/ tests/

      - name: Pytest
        run: pixi run test
